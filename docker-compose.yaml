
version: "3.9"

services:

  veec-comm-generator:
    image: veec-comm-generator:vXXXX.XX.XX
    container_name: veec-comm-generator
    restart: unless-stopped
    volumes:
      - .:/app
    ports:
      - "8000:8000"
